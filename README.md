# GridClicker (AllInKeyboard)

**GridClicker** 是一款专为 macOS 极客打造的**全键盘鼠标控制工具**。

v1.2.0 引入了 **Persistent Control** 模式与 **Status Overlay**，大幅提升了连续操作的效率，让复杂的鼠标任务完全通过键盘完成。

---

## 🚀 核心特性

- **Passive Overlay**: 完美的“无焦点”操作，不抢占底层应用焦点。
- **Status Hint**: 右上角实时显示当前模式的操作快捷键指南。
- **Continuous Control**: 点击后不自动退出，支持连续的 HJKL 微调与多次点击。
- **Safety First**: 优化的底层事件拦截逻辑，确保键盘响应灵敏且不粘连。

---

## 🎮 核心操作流

1.  **唤醒**: 轻敲 **Left Command**。立即显示全屏坐标网格。
2.  **定位**: 输入两个字母（如 `AM`）跳转光标并进入**微调模式**。
3.  **连续操作 (v1.2.0 新增)**:
    *   **移动**: 使用 `HJKL` 进行像素级精准移动。
    *   **点击**: 按 `Space` (左键) 或 `M` (右键)。操作后界面**依然保留**。
    *   **滚动**: 按 `O` 开启滚动模式，使用 `J/K` 滚动。
    *   **状态提示**: 观察右上角提示框，随时了解当前可用按键。
4.  **退出**: 任务完成后，按 **ESC** 隐藏界面并恢复普通键盘输入。

---

## 🛠️ 编译与自定义

如果你修改了 `src/main.swift`，请运行构建脚本并重新部署：

```bash
./scripts/build.sh
sudo cp -R ./dist/GridClicker.app /Applications/
sudo xattr -cr /Applications/GridClicker.app
killall GridClicker
open /Applications/GridClicker.app
```

---

## ❓ 常见问题 (FAQ)

### 1. 更新或移动 App 后无法唤醒？
如果你重新编译了 App 或者将它覆盖到了 `/Applications`，macOS 可能会重置其权限或将其标记为“损坏”。

**解决方法：**
1.  **清除隔离标记** (修复“已损坏”提示)：
    ```bash
    xattr -cr /Applications/GridClicker.app
    ```
2.  **重置辅助功能权限**：
    前往 `系统设置` -> `隐私与安全性` -> `辅助功能`，**删除 (按减号)** 列表中的 `GridClicker`，然后重新运行 App 并再次添加。
3.  **重启进程**：
    ```bash
    killall GridClicker
    open /Applications/GridClicker.app
    ```

---
*Generated by Gemini Agent | 2026.02.11*
